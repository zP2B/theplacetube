extends layout
block append styles
    link(rel='stylesheet' href='/stylesheets/join.css')

block content
    div(class='wrapper')
        form#joinform(class='form-join' method='POST')
            h2(class="form-join-heading") Join the club
            hr
            div(class='form-group')
                label(for="username") Choose a username
                input#username(
                type="text"
                name="username"
                value="" + (undefined === user ? '' : user.username)
                class="form-control "+ ((undefined === errors) ? '' : (undefined === errors.username ? 'is-valid': 'is-invalid'))
                placeholder="Username"
                required=""
                autofocus=""
                )
                if (errors && errors.username)
                    div(class="invalid-feedback") #{errors.username.msg}
                small#usernameHelp(class="form-text text-muted") This will be your public name for the community.
            div(class='form-group')
                label(for="email") Your email address
                input#email(
                type="text"
                name="email"
                value="" + (undefined === user ? '' : user.email)
                class="form-control " + ((undefined === errors) ? '' : (undefined === errors.email ? 'is-valid' : 'is-invalid'))
                placeholder="Email Address"
                required=""
                )
                if (errors && errors.email)
                    div(class="invalid-feedback") #{errors.email.msg}
                small#emailHelp(class="form-text text-muted") We'll never share your email with anyone else.
            div(class='form-group')
                label(for="password") Choose a password
                input#password(
                type="password"
                name="password"
                class="form-control " + ((undefined === errors) ? '' : (undefined === errors.password ? 'is-valid' : 'is-invalid'))
                placeholder="Password"
                required=""
                )
                if (errors && errors.password)
                    div(class="invalid-feedback") #{errors.password.msg}
                //small#passwordHelp(class="form-text text-muted") Your password must be 8-20 characters long.
            div(class='form-group')
                label(for="passwordConf") Confirm the password
                input#passwordConf(
                type="password"
                name="passwordConf"
                class="form-control " + ((undefined === errors) ? '' : (undefined === errors.passwordConf ? 'is-valid' : 'is-invalid'))
                placeholder="Password confirmation"
                required=""
                )
                if (errors && errors.passwordConf)
                    div(class="invalid-feedback") #{errors.passwordConf.msg}

            button(class="btn btn-lg btn-primary btn-block" type="submit") Submit
            a(href="/users/login" class="btn btn-block" role="button" aria-label="login") You are already member of the club?
